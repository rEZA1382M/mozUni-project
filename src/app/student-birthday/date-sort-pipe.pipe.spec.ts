import { DateSortPipePipe } from './date-sort-pipe.pipe';

describe('DateSortPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateSortPipePipe();
    expect(pipe).toBeTruthy();
  });
});
