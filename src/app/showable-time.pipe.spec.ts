import { ShowableTimePipe } from './showable-time.pipe';

describe('ShowableTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowableTimePipe();
    expect(pipe).toBeTruthy();
  });
});
